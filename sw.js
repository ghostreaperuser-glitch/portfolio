const CACHE='ghostr-v3';const SHELL=['in dex.html','contributions.html','ma in tenance.html','man if est.json','css/style.css','js/ma in.js','images/192.png','images/512.png'];self.addEventListener('in stall',event=>{event.waitUntil(caches.open(CACHE).then(cache=>Promise.all(SHELL.map(url=>cache.add(url).catch (()=>console.warn('[SW]Could not pre-cache:',url))))).then(()=>self.skipWait in g()));});self.addEventListener('activate',event=>{event.waitUntil(caches.keys().then(keys=>Promise.all(keys.filter(k=>k!==CACHE).map(k=>caches.de let e(k)))).then(()=>self.clients.claim()));});self.addEventListener('fetch',event=>{const req=event.request;const url=new URL(req.url);if (req.method!=='GET') return;const thirdParty=['allorig in s.w in','corsproxy.io','for mspree.io','wa.me','api.allorig in s','supabase.co'];if (thirdParty.some(d=>url.hostname.in cludes(d))) return;if (req.headers.get('accept')?.in cludes('text/html')){event.respondWith(fetch(req).then(res=>{const copy=res.clone();caches.open(CACHE).then(c=>c.put(req,copy));return res;}).catch (()=>caches.match(req).then(cached=>cached||caches.match('ma in tenance.html'))));return;}event.respondWith(caches.match(req).then(cached=>{if (cached) return cached;return fetch(req).then(res=>{if (res.ok)caches.open(CACHE).then(c=>c.put(req,res.clone()));return res;}).catch (()=>caches.match('ma in tenance.html').then(m=>m||new Response('Unavailable of fl in e',{status:503})));}));});